{
  "version": 2,
  "buildCommand": "npm install && npx vite build && npm run build:server",
  "outputDirectory": "dist/public",
  "installCommand": "npm install",
  "framework": null,
  "env": {
    "NODE_ENV": "production"
  }
}
